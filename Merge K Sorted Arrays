#include <bits/stdc++.h> 
vector<int> mergeKSortedArrays(vector<vector<int>>&karr, int k)
{  
    vector<int> newarr;
    for(int i=0;i<k;i++){ 
        newarr.insert(newarr.end(),karr[i].begin(),karr[i].end());
    }
    sort(newarr.begin(),newarr.end());
    return newarr;
}
